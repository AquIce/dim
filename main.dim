struct {
	age: i32;
	name: str;
} User;

impl User {
	fn greet(predicate: str) -> str {
		return predicate + this.name;
	}
};

fn test() -> i32 {
	return 0;
}

main {
	var steve := User {
		.name = "Steve",
		.age = 21,
	};
	steve.greet("Hello, ");
	test();
};
