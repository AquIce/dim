fn nthFibonacci(n: u32) -> u32 {

    // Base case
    if(n <= 1) {
        return n;
    } else 0;

    // Recursive case: sum of the two preceding Fibonacci numbers
    return nthFibonacci(n - 1) + nthFibonacci(n - 2);
}

main {
    var n := 7;
    nthFibonacci(n);
};